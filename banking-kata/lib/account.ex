defmodule BankingKata.Account do
  @spec deposit(Account, Integer) :: Account
  def deposit(account, amount) do
    account
  end

  @spec withdraw(Account, integer) :: Account
  def withdraw(account, amount) do
    account
  end

  @spec printStatement(Account) :: Nil
  def printStatement(account) do
  end
end
